# OrbitEngine

Cloud platform where you open a chat alongside an engine, link your GitHub, and
the engine reads/writes repositories, PRs, and issues, bootstraps new projects,
edits code, runs tests, and pushes fixes. All execution happens in the cloud.

Status: early-stage, pre-decisions. See AGENTS.md.
