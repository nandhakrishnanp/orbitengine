# Next.js frontend

The chat + status client is a Next.js app. Chosen over a plain Vite SPA to keep
the entire product — client, backend, sandboxes — on the Vercel platform with a
single framework. The frontend was the last undecided stack item.