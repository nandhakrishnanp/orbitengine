# TypeScript/Node for the cloud service

The cloud service that runs engine loops, orchestrates sandboxes, and talks to
GitHub is built in TypeScript on Node. Chosen over Python for the strongest
GitHub (Octokit) and agent/AI SDK ecosystem, and to keep one language across
client and server. Vercel Sandbox's JS SDK reinforces the choice.